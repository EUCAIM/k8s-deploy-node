kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: guacamole-postgresql
  namespace: guacamole 
spec:
  storageClassName: cephfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi

