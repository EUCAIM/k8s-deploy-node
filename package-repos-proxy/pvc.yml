kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: devpi-pvc
  namespace: package-repos-proxy
spec:
  storageClassName: cephfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi