---
apiVersion: v1
kind: Service
metadata:
  name: kubernetes-dashboard-direct-access
spec:
  type: NodePort
  ports:
  - nodePort: 30443
    port: 443
    targetPort: 8443
  selector:
    app.kubernetes.io/component: app
    app.kubernetes.io/instance: kubernetes-dashboard
    app.kubernetes.io/name: kong
